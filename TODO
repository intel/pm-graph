add switch options to hide/show device blocks
add suspend start and resume end lines to timescale
add timeline phase/device outline to the detail popup
embed the detail popup in the page itself

include dev functions that fall outside of devices
- add new async thread bar in section 2
- add the calls to this thread

allow ftrace through all of x2
- add option to select which test run to graph
- add option to filter out all but a specific phase

handle crashes better:
- include a crash graphically in the timeline
- remove firmware time if the resume actually failed

add commit id to html output
add an error section on the page (instead of on the command line)
format ftrace/dmesg pages to use preformat text
